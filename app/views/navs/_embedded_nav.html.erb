<nav class="navbar navbar-expand-lg navbar-light bg-white ml-3 mt-2">
  <a class="navbar-brand" href="#">The Shop App</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item mx-3">
        <a href="<%= admin_url %>" class="nav-link text-muted <%= params[:controller] == 'home' && params[:action] == 'index' ? 'is-active' : '' %>">
          Dashboard
        </a>
      </li>
    </ul>
  </div>
</nav>